# 项目乱码修复报告

> 执行时间：2026-09-11　范围：全项目 2600+ 文本文件　结果：**0 处残留**

## 一、结论

全项目扫描后定位并修复 **12 个文件**的中文乱码，复检为 0 处残留。
乱码共 3 种类型，全部为「编码解读错误」，非文件损坏：

| 类型 | 表现 | 成因 | 修复处数 |
|---|---|---|---|
| A 双重编码 | `ç”¨æˆ·` → `用户` | UTF-8 字节被误按 cp1252/latin1 解读后存成 UTF-8 | 311 |
| B GBK 误读 | `灏婃暚鐨勭敤鎴` → `尊敬的用户` | UTF-8 字节被误按 GBK 解读 | 25 |
| C 整体非 UTF-8 | 整文件乱码 | 文件本身就是 GBK 编码 | 1（38MB 日志） |

## 二、已修复文件清单

| 文件 | 修复内容 |
|---|---|
| `server -schema-current.sql` | 311 处；299 条字段注释全部还原（收货人姓名 / 详细地址 / 门牌号等补充信息 / 公司 等），0 异常字符 |
| `docs/database/full-schema.sql` | 表注释 `资质证明`→`资质证明表`；索引注释还原为 `所属主体索引` |
| `pet-ai/.../AgentChatRequestDTO.java` | `消息不能为空`、`用户输入的消息`、`历史记录列表` |
| `pet-ai/.../AgentChatResponseDTO.java` | `回答内容`、`对话ID`、`结束原因` |
| `pet-ai/.../RagDocumentCreateRequestDTO.java` | `文档标题`、`文档内容`、`文档内容不能为空` |
| `pet-business/.../TicketCreateRequestDTO.java` | `工单标题不能为空`、`工单内容不能为空` |
| `pet-business/.../ServiceItemUpdateImagesRequestDTO.java` | `图片`、`图片URL不能为空` |
| `pet-admin/backend-run.log` | 38.4MB 日志整体 GBK → UTF-8（93578 个中文字符） |
| `create_word_docs.py` | 源码中裸的 U+FFFD 替换字符 → 转义写法 `'\ufffd?'` |
| `test_clean.py` | 同上 |
| `test_clean2.py` | 同上 |
| `.playwright-mcp/page-2026-09-10T15-30-28-911Z.yml` | 临时快照，6 处已还原 |

## 三、防复发措施

1. **`.editorconfig`（新增）** — 声明 `charset = utf-8`，编辑器统一按 UTF-8 读写。
2. **`start-app.bat` / `start-backend.bat`** — 增加 `chcp 65001` 与 `-Dfile.encoding=UTF-8 -Dstdout.encoding=UTF-8 -Dstderr.encoding=UTF-8`。
   这是后端日志乱码的根因：Java 17 在 Windows 下默认按 GBK 输出控制台/重定向流。
3. **`AGENT_RULES.md`** — 新增规则 22：所有文本文件必须 UTF-8，禁止提交含乱码或 U+FFFD 的文件。
4. **`fix_encoding.py`（新增，项目根）** — 编码体检工具，可随时复查：
   - `python fix_encoding.py` —— 只扫描并输出报告，不改文件
   - `python fix_encoding.py --apply` —— 执行修复（自动 `.bak` 备份）

## 四、注意事项

- **备份**：所有被改文件均生成同名 `.bak`，合计约 39MB。确认无误后可自行删除。
  其中 `server -schema-current.sql.bak` 为多次执行的中间态，非最初乱码原版。
- **不可恢复处**：`.playwright-mcp/*.yml` 是 Playwright 临时快照，其中若干 `?` 为原始字节永久丢失，无法还原；
  该目录已被 `.gitignore` 忽略，内容亦会被下次运行覆盖，可忽略。
- **数据库内的存量数据**：本报告只修复磁盘文件。若线上库中已有写坏的字段值，需另行用 SQL 清洗。
