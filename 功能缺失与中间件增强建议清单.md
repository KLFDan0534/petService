# 功能缺失与中间件增强建议清单

> 记录时间：2026-09-05
> 用途：记录当前项目里"功能缺失 / 可以引入 Redis 等中间件增强"的点，供后续自行实现。
> 说明：以下均为**建议**，不是已存在功能。建议中给出的文件路径是当前项目中相对项目根目录的路径，供定位参考。

---

## 一、当前中间件落地现状（已有，仅供参考）

redis的list,unread,公告/通知,
需要删除serviceItem里面的无用重复方法,并且查看整个项目有没有没用的方法

| 中间件 | 已落地场景 | 相关文件 |
|--------|-----------|---------|
| Redis | JWT 黑名单注销、注册验证码 `register_captcha:{phone}`(TTL 300s)、登录失败锁定 `login_fail:{username}`(15min)、登出状态 `logout:user:{userId}` | `pet-system/src/main/java/com/pet/system/service/impl/UserServiceImpl.java`、`pet-security/src/main/java/com/pet/security/JwtAuthenticationFilter.java` |
| RabbitMQ | 订单支付超时延迟队列、接单超时延迟队列、统一事件总线 `pet.direct` | `pet-framework/src/main/java/com/pet/mq/MessageSender.java`、`MessageListener.java`、`pet-business/src/main/java/com/pet/order/mq/OrderPaymentTimeoutListener.java` |
| MinIO | 商品/服务图片媒体存储 + 版本迁移 | `pet-admin/src/main/resources/application.yml` |
| ChromaDB | pet-ai 知识库向量检索（RAG） | `pet-ai` 模块 |
| AOP 审计 | `@LogOperation` 操作日志切面 | `pet-admin/src/main/java/com/pet/admin/aspect/LogOperationAspect.java` |

---

## 二、功能缺失 / 增强建议（按优先级排序，从高到低）

### 1. 【高】Redis 目前只做"状态"，没做"缓存"
现状的关键事实：当前项目 Redis 里**没有任何长期缓存数据**，只有带短 TTL 的临时状态（验证码、失败计数、黑名单）。
建议：为高频只读数据引入 Spring Cache（`spring-boot-starter-cache`）+ `@Cacheable`，或 Caffeine 本地 + Redis 两级缓存。
适合缓存（**只读 / 变更少的**）：
- 首页服务列表、服务分类 / `category`、商户列表
- 服务套餐、基础字典数据、公告列表

⚠️ 不适合缓存：订单、余额、推荐/实时状态等强一致数据，切忌随意缓存，否则脏读。

### 2. 【高】分布式限流缺失（现有 AI 限流是单机版，会失效）
现状：`pet-business/src/main/java/com/pet/ai/service/impl/AiChatServiceImpl.java` 用 `ConcurrentHashMap` 做用户级滑动窗口（`RATE_LIMIT_PER_MINUTE=20`）。
问题：这是**进程内**限流，部署多个实例后各自独立统计，限流会失效。
建议：改用基于 Redis 的滑动窗口或令牌桶；引入 redisson 的 `RRateLimiter`，或自定义 `SETNX + expire` 滑动窗口。

### 3. 【高】缺少接口级全局限流 / 防刷（目前只有登录失败这一个防刷点）
现状：只有登录失败锁定（`login_fail:{username}`），其余接口无防刷。
建议：自定义 `@RateLimit` 注解 + Redis + AOP 切面。
重点保护接口：验证码、AI 对话、支付/充值、提现、举报、下单。

### 4. 【高】缺少分布式锁（单机没问题，多实例并发会出错）
现状：**全项目未发现任何分布式锁**。
风险场景（并发下余额/库存可能被扣超或重复操作）：
- finance 余额扣减 / 充值 / 提现
- 优惠券领取（防超发）
- 接单 / 抢单（防双人同时接同一单）
- 商品/库存扣减

建议：引入 `redisson`（重入锁 + 看门狗自动续期），或 `RedisTemplate` + `SETNX` 自实现锁，结合 lua 保证原子性。

### 5. 【中】RabbitMQ 事件总线已建，可进一步异步解耦
现状：订单事件已走队列（下单/取消/支付超时/接单超时）。
可扩展（避免主链路阻塞）：
- 下单成功 / 服务完成 → 站内信 + 邮件通知（`spring-boot-starter-mail` 已在 pom 引入，待用）
- 评价完成 → 积分 / 会员成长值异步增加
- 统计数据异步落库（统计接口读取时不用现场聚合）

### 6. 【中】使用缓存时的缓存一致性（Cache Evict 成对清）
若按 #1 做了缓存，务必成对清理：
- 订单状态变更 → 清对应用户的订单列表缓存
- 优惠券领取 → 清优惠券余量缓存
- 商户/服务上下架 → 清相关列表缓存

### 7. 【低】集群部署下会话在线 / 踢人下线能力
现状：JWT 无状态 + Redis 黑名单，已支持多实例。
可增强：在 Redis 存 `用户ID → 当前token` 映射，实现"顶号下线 / 强制踢人 / 异地登录提醒"。

---

## 三、当前 Redis 为空的原因（正常现象）

当前项目 Redis 里**确实没有任何长期数据**，属于预期行为，不是配置错误。原因：
1. Redis 只用于存储**带短 TTL 的一次性临时状态**，正常静置状态是空的：
   - 注册验证码 `register_captcha:*` → 300 秒过期后消失
   - 登录失败计数 `login_fail:*` → 锁定释放后清除
   - JWT 黑名单 / 登出 `blacklist:token:*` / `logout:user:*` → 登出/失效才有，且 TTL 不长
2. 这些数据都是"**事件触发才写入**"：
   - 你没注册/发验证码 → 无 `register_captcha`
   - 没反复输错密码触发锁定 → 无 `login_fail`
   - 没登出 → 无 `blacklist / logout`
3. 项目**没有**把任何查询结果缓存进 Redis（这正是第 #1 条缺失点），所以正常访问业务页面也不会产生缓存 key。
4. 请确认你连的是：
   - 端口 `6379`（Docker 容器 `pet-redis`）
   - database `0`（application.yml 里 `redis.database: 0`）
   若连的是别的端口或别的 db，也会看到为空。

**验证方式（可自行操作，注意只是提建议不替你执行）：**
- 在 Redis Desktop 里执行 `KEYS *` 或 `SCAN 0` 看有没有 key；
- 故意触发一次接口（如注册页请求验证码）后，应短暂看到 `register_captcha:*`。

---

> 结论：项目本身很完整；Redis 为空不是 bug，是因为当前 Redis 只承担"临时状态"职责、且这些状态需事件触发才有。若希望 Redis 有"常驻数据"，最值得做的就是上面第 #1 条（给只读热点加缓存）。