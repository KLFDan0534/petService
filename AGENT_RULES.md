# AI Development Rules

## 基本原则
1. 先设计文档，再写代码
2. 先写接口，再写前端
3. 先完成一个模块，再做下一个
4. 每完成模块必须自动测试
5. 不允许存在 TODO
6. 不允许存在 Mock 数据
7. 不允许出现 undefined
8. 不允许出现空页面
9. 不允许出现 500 错误
10. 不允许跳过权限校验
11. 不允许直接操作数据库绕过业务层
12. 所有图片视频必须走 MinIO
13. 所有异步通知必须走 RabbitMQ
14. 所有AI问答必须走RAG知识库
15. 所有代码提交Git
16. 每个模块完成必须生成MD文档
17. 更新项目进度文档
18. 更新数据库设计文档
19. 更新接口文档(Swagger)
20. 所有功能通过浏览器自动化测试后才允许进入下一阶段
21. 所有新增数据库字段、实体字段、DTO/VO字段、请求/响应JSON字段必须使用 `_wsh` 后缀，禁止新增不带 `_wsh` 后缀的业务字段；兼容旧字段时只能作为临时适配层存在
22. 所有文本文件必须使用 UTF-8 编码（禁止 GBK/GB18030），禁止提交含乱码或替换字符（U+FFFD）的文件；新增文件不得引入非 UTF-8 字节

## 实施顺序
01 用户模块
02 RBAC权限模块
03 宠物模块
04 商家模块
05 寄养员模块
06 服务模块
07 收藏模块
08 订单模块
09 支付模块
10 退款模块
11 投诉模块
12 聊天模块
13 MinIO模块
14 RabbitMQ模块
15 数据统计模块
16 RAG知识库
17 AI照护建议
18 AI寄养报告
19 Agent自动下单
20 全链路测试
